/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.pinimg.com",
      "plus.unsplash.com",
      "images.unsplash.com",
      "www.shutterstock.com",
      "res.cloudinary.com",
    ],
  },
};

export default nextConfig;
